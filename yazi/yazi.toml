[mgr]
linemode = "size_and_mtime"
ratio = [1, 3, 2]
show_hidden = true
show_symlink = true
sort_by = "natural"
sort_dir_first = true
sort_reverse = true
sort_sensitive = true

[preview]
image_delay = 0
max_height = 5000
max_width = 5000
tab_size = 2
ueberzug_offset = [0, 0, 0, 0]
ueberzug_scale = 1

[opener]
edit = [
  { run = 'nvim %s', block = true, for = "macos" },
  { run = 'nvim %s', block = true, for = "unix" },
  { run = 'code %s', orphan = true, for = "windows" },
]
extract = [
  { run = 'unar %s1', desc = "Extract here", for = "unix" },
  { run = 'unar %s1', desc = "Extract here", for = "windows" },
]
open = [
  { run = 'xdg-open %s', desc = "Open", for = "linux" },
  { run = 'open %s', desc = "Open", for = "macos" },
  { run = 'start "" %s1', orphan = true, desc = "Open", for = "windows" },
]
play = [
  { run = 'mpv --vo=libmpv --geometry=50% --no-border --focus-on=open --mute=yes %s', orphan = true, for = "unix" },
  { run = 'mpv %s1', orphan = true, for = "windows" },
  { run = '''mediainfo %s1; echo "Press enter to exit"; read''', block = true, desc = "Show media info", for = "unix" },
]
reveal = [
  { run = 'open -R %s1', desc = "Reveal", for = "macos" },
  { run = 'explorer /select, %s1', orphan = true, desc = "Reveal", for = "windows" },
  { run = '''exiftool %s1; echo "Press enter to exit"; read''', block = true, desc = "Show EXIF", for = "unix" },
]
rootbrowse = [{ run = 'rootbrowse %s1', orphan = true, for = "unix" }]
root = [{ run = '~/dotfiles/scripts/run_root_macro.sh %s1', for = "unix", desc = "root -l" }]
sioyek = [{ run = 'sioyek %s1', orphan = true, for = "unix" }]

[open]
prepend_rules = [
  { mime = "application/pdf", use = [
    "sioyek",
    "reveal",
  ] },
  { url = "*.root", use = [
    "rootbrowse",
  ] },
  { url = "*.C", use = [
    "root",
  ] },
]

[tasks]
bizarre_retry = 5
image_alloc = 536870912  # 512MB
image_bound = [0, 0]
file_workers = 10
plugin_workers = 10
fetch_workers = 5
preload_workers = 10
process_workers = 5
suppress_preload = false

[plugin]
prepend_preloaders = [
  { url = "${HOME}/patricorgi/SSHFS/**", run = "noop" },
  { url = "/Volumes/**", run = "noop" },
  { mime = 'application/pdf', run = "noop" },
]
prepend_previewers = [{ url = "${HOME}/patricorgi/SSHFS/**", run = "noop" }]
previewers = [
  { url = "*/", run = "folder" },
  # Code
  { mime = "text/*", run = "code" },
  { mime = "*/xml", run = "code" },
  { mime = "*/javascript", run = "code" },
  { mime = "*/x-wine-extension-ini", run = "code" },
  # JSON
  { mime = "application/json", run = "json" },
  # Image
  { mime = "image/djvu", run = "noop" },
  { mime = "image/*", run = "image" },
  # Video
  { mime = "video/*", run = "video" },
  # PDF
  { mime = "application/pdf", run = "pdf" },
  # Archive
  { mime = "application/zip", run = "archive" },
  { mime = "application/gzip", run = "archive" },
  { mime = "application/x-tar", run = "archive" },
  { mime = "application/x-bzip", run = "archive" },
  { mime = "application/x-bzip2", run = "archive" },
  { mime = "application/x-7z-compressed", run = "archive" },
  { mime = "application/x-rar", run = "archive" },
  { mime = "application/xz", run = "archive" },
]

[input]
# cd
cd_offset = [0, 2, 50, 3]
cd_origin = "top-center"
cd_title = "Change directory:"

# create
create_offset = [0, 2, 50, 3]
create_origin = "top-center"
create_title = ["Create:", "Create (dir):"]

# rename
rename_offset = [0, 1, 50, 3]
rename_origin = "hovered"
rename_title = "Rename:"

# trash
trash_offset = [0, 2, 50, 3]
trash_origin = "top-center"
trash_title = "Move {n} selected file{s} to trash? (y/N)"

# delete
delete_offset = [0, 2, 50, 3]
delete_origin = "top-center"
delete_title = "Delete {n} selected file{s} permanently? (y/N)"

# filter
filter_offset = [0, 2, 50, 3]
filter_origin = "top-center"
filter_title = "Filter:"

# find
find_offset = [0, 2, 50, 3]
find_origin = "top-center"
find_title = ["Find next:", "Find previous:"]

# search
search_offset = [0, 2, 50, 3]
search_origin = "top-center"
search_title = "Search:"

# shell
shell_offset = [0, 2, 50, 3]
shell_origin = "top-center"
shell_title = ["Shell:", "Shell (block):"]

# overwrite
overwrite_offset = [0, 2, 50, 3]
overwrite_origin = "top-center"
overwrite_title = "Overwrite an existing file? (y/N)"

# quit
quit_offset = [0, 2, 50, 3]
quit_origin = "top-center"
quit_title = "{n} task{s} running, sure to quit? (y/N)"

[select]
open_offset = [0, 1, 50, 7]
open_origin = "hovered"
open_title = "Open with:"

[log]
enabled = false