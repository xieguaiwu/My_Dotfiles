# 使用 Super 键作为修饰键
set $mod Mod4

# 启动终端（wezterm）
bindsym $mod+Return exec wezterm

# 关闭窗口
bindsym Ctrl+q kill

# 窗口平铺/浮动切换
bindsym $mod+Shift+space floating toggle

# 窗口全屏
bindsym $mod+f fullscreen toggle

# 焦点移动 (vim 风格)
bindsym $mod+h focus left
bindsym $mod+j focus down
bindsym $mod+k focus up
bindsym $mod+l focus right

# 窗口移动
bindsym $mod+Shift+h move left
bindsym $mod+Shift+j move down
bindsym $mod+Shift+k move up
bindsym $mod+Shift+l move right

# 工作区
set $ws1 "1"
set $ws2 "2"
set $ws3 "3"
set $ws4 "4"
set $ws5 "5"
set $ws6 "6"
set $ws7 "7"
set $ws8 "8"
set $ws9 "9"

bindsym $mod+1 workspace $ws1
bindsym $mod+2 workspace $ws2
bindsym $mod+3 workspace $ws3
bindsym $mod+4 workspace $ws4
bindsym $mod+5 workspace $ws5
bindsym $mod+6 workspace $ws6
bindsym $mod+7 workspace $ws7
bindsym $mod+8 workspace $ws8
bindsym $mod+9 workspace $ws9

bindsym $mod+Shift+1 move container to workspace $ws1
bindsym $mod+Shift+2 move container to workspace $ws2
bindsym $mod+Shift+3 move container to workspace $ws3
bindsym $mod+Shift+4 move container to workspace $ws4
bindsym $mod+Shift+5 move container to workspace $ws5
bindsym $mod+Shift+6 move container to workspace $ws6
bindsym $mod+Shift+7 move container to workspace $ws7
bindsym $mod+Shift+8 move container to workspace $ws8
bindsym $mod+Shift+9 move container to workspace $ws9

# 状态栏
exec_always --no-startup-id waybar

# input method
exec_always --no-startup-id fcitx5

# 启动 picom（透明/阴影）
exec_always --no-startup-id picom --config /home/xieguaiwu/.config/picom.conf

# 设置壁纸
exec_always --no-startup-id feh --bg-scale /usr/share/backgrounds/fedora-workstation/futurecity_dark.webp

# 使用 rofi 作为启动器 (Super+D)
bindsym $mod+d exec rofi -show drun

# 窗口切换器 (Super+Tab)
bindsym $mod+Tab exec rofi -show window

# start vpn
exec_always --no-startup-id clash-verge

# Touchpad
exec --no-startup-id xinput set-prop "Elan Touchpad" "libinput Natural Scrolling Enabled" 1
exec --no-startup-id xinput set-prop "Elan Touchpad" "libinput Tapping Enabled" 1

# -------------------- 音频控制 --------------------
# 音量增大
bindsym XF86AudioRaiseVolume exec --no-startup-id pactl set-sink-volume @DEFAULT_SINK@ +1%

# 音量减小
bindsym XF86AudioLowerVolume exec --no-startup-id pactl set-sink-volume @DEFAULT_SINK@ -1%

# 扬声器/主音量静音/取消静音
bindsym XF86AudioMute exec --no-startup-id pactl set-sink-mute @DEFAULT_SINK@ toggle

# 麦克风静音/取消静音
bindsym XF86AudioMicMute exec --no-startup-id pactl set-source-mute @DEFAULT_SOURCE@ toggle

# -------------------- 亮度控制 (使用 brightnessctl) --------------------
# 亮度增加
bindsym XF86MonBrightnessUp exec --no-startup-id brightnessctl set +1%

# 亮度减小
bindsym XF86MonBrightnessDown exec --no-startup-id brightnessctl set 1%-
