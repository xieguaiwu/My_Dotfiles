# Secrets / tokens
pi-agent/auth.json
url.txt

# Large binaries (not configs)
*.apkg
*.colpkg

# macOS artifacts
.DS_Store

# Editor temp files
*.swp
*.swo
*~

# pi-agent: exclude npm packages and session history
pi-agent/npm/
pi-agent/sessions/

# Environment files with keys
.env
.env.*

# Cache / log / database files (not config)
*.log
*.dat
*.mb
*.db
fcitx/clipboard/
fcitx/log/

# pi 运行时目录
.pi-subagents/

# Python 缓存
__pycache__/

# 敏感凭据（已移出跟踪，历史残留待 BFG）
alpine.passfile
pulse/cookie
sunshine/credentials/cakey.pem

# pi web-search 配置（tavily key 走环境变量 TAVILY_API_KEY，禁止明文入库）
.pi/web-search.json
